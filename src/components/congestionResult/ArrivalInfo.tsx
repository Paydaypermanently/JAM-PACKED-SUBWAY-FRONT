import styled from 'styled-components'

interface IProps {
  congestion: ITrainCongestion
}

function ArrivalInfo({congestion}: IProps) {
  return <Wrapper>arrivalInfo</Wrapper>
}

export default ArrivalInfo

const Wrapper = styled.div``
